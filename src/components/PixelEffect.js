// PixelEffect.js
import styled from 'styled-components';

const PixelEffect = styled.div`
  image-rendering: pixelated;
  transform: translate(10px, 10px); /* Ajusta el valor según sea necesario para el tamaño deseado de píxeles */
  transform-origin: top left;
`;
export default PixelEffect;
